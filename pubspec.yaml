name: tsuruo_kit
description: A toolkit of convenient widgets, extensions, providers etc created by tsuruo.
version: 0.0.14
repository: https://github.com/HTsuruo/flutter_tsuruo_kit

environment:
  sdk: '>=2.18.0 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  cloud_firestore: ^4.7.1
  # due to `flutter_test` depends on collection 1.17.1
  collection: ^1.17.1
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.3.6
  flutter_web_plugins:
    sdk: flutter
  rxdart: ^0.27.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono: ^1.22.0
